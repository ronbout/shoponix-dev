import Router from "next/router";

const ProfileClub = (props) => {
  Router.push("/profile-club-fees");
  return (
    <div>
      <h1>Club Profile Form</h1>
    </div>
  );
};

export default ProfileClub;
